import lambda = require('@aws-cdk/aws-lambda');
import cdk = require('@aws-cdk/cdk');
export declare class TestFunction extends lambda.Function {
    constructor(parent: cdk.Construct, id: string);
}
